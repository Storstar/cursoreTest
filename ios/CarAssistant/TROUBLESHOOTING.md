# Решение проблем с OpenAI API

## Проблема: Сообщение не появляется в чате

### Возможные причины:

1. **Модель недоступна** - модель `gpt-4` может быть недоступна для вашего API ключа
2. **API ключ не имеет доступа** - некоторые ключи имеют ограничения
3. **Проблемы с сетью** - OpenAI API может быть недоступен в РФ

## Решение 1: Проверка модели

Откройте `CarAssistant/Utils/AIService.swift` и измените модель:

```swift
// Попробуйте одну из этих моделей:
private let model = "gpt-4"              // Самая мощная (может быть платной)
private let model = "gpt-3.5-turbo"      // Более доступная модель
private let model = "gpt-4-turbo"        // Альтернатива gpt-4
```

**Рекомендация:** Начните с `gpt-3.5-turbo` - она доступна для большинства ключей.

## Решение 2: Проверка логов

В Xcode откройте консоль (View → Debug Area → Activate Console) и отправьте сообщение. Вы увидите:

- `📤 Отправка запроса к OpenAI...` - запрос отправляется
- `🌐 Отправка HTTP запроса к OpenAI API...` - HTTP запрос отправлен
- `📥 HTTP статус: 200` - успешный ответ
- `❌ HTTP ошибка: 401` - неверный API ключ
- `❌ HTTP ошибка: 404` - модель не найдена
- `❌ HTTP ошибка: 429` - превышен лимит запросов

## Решение 3: Проверка API ключа

1. Откройте [OpenAI Platform](https://platform.openai.com/)
2. Перейдите в [API Keys](https://platform.openai.com/api-keys)
3. Убедитесь, что ключ активен
4. Проверьте лимиты использования

## Решение 4: Проверка доступности API в РФ

OpenAI API может быть недоступен напрямую из РФ. Решения:

1. **Использовать VPN** (для тестирования)
2. **Использовать прокси-сервер** (для продакшена)
3. **Использовать альтернативные API** (Anthropic Claude, Google Gemini)

## Типичные ошибки:

### Ошибка 401: Unauthorized
- **Причина:** Неверный API ключ
- **Решение:** Проверьте ключ в `Info.plist`

### Ошибка 404: Model not found
- **Причина:** Модель недоступна для вашего ключа
- **Решение:** Измените модель на `gpt-3.5-turbo`

### Ошибка 429: Rate limit exceeded
- **Причина:** Превышен лимит запросов
- **Решение:** Подождите или увеличьте лимит в настройках OpenAI

### Ошибка: Network error
- **Причина:** Нет подключения к интернету или API недоступен
- **Решение:** Проверьте интернет-соединение

## Что изменилось:

1. ✅ Сообщение пользователя теперь сохраняется **сразу** и появляется в чате
2. ✅ Если API не отвечает, показывается сообщение об ошибке
3. ✅ Добавлено подробное логирование для отладки
4. ✅ Модель изменена на `gpt-4` (можно переключить на `gpt-3.5-turbo`)

## Проверка работы:

1. Откройте приложение
2. Откройте консоль Xcode (⌘+Shift+Y)
3. Отправьте сообщение в чате
4. Проверьте логи в консоли

Если видите ошибку - скопируйте её и проверьте по списку выше.

